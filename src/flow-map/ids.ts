/** 与 electron/shared/map-ids 同源，保证 focus / 投影 id 一致。 */
export {
  NEWS_ROOT_ID,
  subAgentId,
  routeInstanceId,
  routeNodeId,
  mergedClaimNodeId,
  opinionNodeId,
  edgeId,
  verifyInstanceId,
  scopedVerifyInstanceId,
} from '../../electron/shared/map-ids'
