# 042 — 返回格式结构化 + 智能体管理 UI

## 目标

- 返回格式由 `prompt-output` 注册表按 PromptKind 自动拼装
- Split SubAgent 可选 `claimCategory`（data/quote/causal）
- 智能体管理统一列表（含 parse/协调者），左侧类型标签，右侧 endpoint + 类型选择

## 模块

| 模块 | 路径 |
|------|------|
| 返回格式 | `electron/shared/prompt-output.ts` |
| 统一注册 | `electron/api/agent-registry-service.ts` |
| UI | `AgentManagerCanvas.vue` / `PromptOutputPreview.vue` |
