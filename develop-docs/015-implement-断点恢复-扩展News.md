# 015-implement — 断点恢复

1. database schema mapRun/mapGraph
2. types + serialize + news-service persist
3. graph-doc hydrate/persist helpers + nextNode
4. graph-utils skipInitialInvoke
5. graph-service restoreRun + stable threadId
6. adapter persist + ensureGraph hydrate + restore
7. IPC restore channel
