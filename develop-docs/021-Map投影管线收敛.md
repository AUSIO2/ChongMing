# Map 投影管线收敛

## 背景

graph-doc 内 bootstrap / interrupt 投影、draft 排序、claim parent 解析存在分叉与隐式契约。

## 目标

1. 统一 draft 物化顺序（`mapIdReadSubAgentClaim`）
2. 统一 claim 读取（`docReadClaims`）
3. 修复 `docReadClaimParent` 多槽歧义
4. 阶段 0 命名债务（无形容词）

## 非目标

- Electron split/verify 图工厂（见 022）
- MapAPI 接口改名

## 验收

`npm run test:map` 通过
