# 041 — 提示词注入参数结构化

## 目标

- `content` 仅存可编辑指令正文
- `promptVars` 多选启用运行时注入块（`【标题】\n{{var}}`）
- 运行时 `promptRender` 拼装后 `promptFormat`
- 覆盖 subagentconfig 全部 11 个 JSON

## 模块

| 模块 | 路径 |
|------|------|
| 注册表 | `electron/shared/prompt-vars.ts` |
| 迁移 | `electron/shared/prompt-migrate.ts` |
| MainAgent/parse 读写 | `electron/api/prompt-config-service.ts` |
| UI | `PromptVarMultiSelect.vue` / `PromptEditorPanel.vue` |
