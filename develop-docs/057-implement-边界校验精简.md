# 057 — 边界校验精简实施原稿

1. 追踪配置、路由、节点数据、SDK 通知和凭证文件的生产者与调用方，确认入口与内部类型边界。
2. 删除 Graph/Run 重复配置解析及内部路由结构解析，保留数据复制、动态权限和业务不变量。
3. 删除已由白名单覆盖的禁用字段判断；使用标准 JSON 复制取代递归通知转换。
4. 收窄凭证文件读取的异常处理，保留已有明确重新登录场景。
5. 在已有测试中补充上述边界回归，运行相关和全量测试及类型检查。
6. 记录实际代码净减少量，更新说明，只提交本轮文件并按既有约定 push。

## 完成记录

删除 Graph/Run 的两次重复 configurationRead，保留 control.configuration 的一次组装校验；路由结构只在 HTTP 入口解析，内部保留动态工具授权与数量/唯一性检查。冻结配置和 slots 仍使用独立副本。

删除节点白名单后重复的四组禁用字段检查；删除 DSH 自写递归 JSON 转换与任意值字符串化，使用原生 JSON 复制。凭证读取只对不存在或损坏 JSON 返回未登录，其他读盘错误向上传递；解密失败仍要求重新登录。

- 后端与后端 contracts：4,425 → 4,400 行，减少 25 行。
- 加上凭证异常处理新增的 3 行，本轮生产代码净减少 22 行。没有通过压缩排版或删除测试制造减少量。
- 针对性测试：4 文件、28 项通过；全量回归：43 文件、164 项通过。
- vue-tsc --noEmit、契约文档校验、git diff --check 通过。

本轮是具体重复路径清理，不代表所有 fallback 都已逐项证明可删。剩余存储默认值要先核对存量字段；跨 HTTP/IPC/模型边界和业务权限的检查继续保留。未修改旧后端与用户已有未提交文件，未重建或安装桌面发行包。
