# 031 — Map 投影阶段统一

## 背景

LangGraph 三列共用 `graphBuildHitl`（confirmRoute → subAgent×N → merge → validate → save），Map 层投影仍散落在 `graph-doc.ts` 的 if/else 中；validate 中断误 prune 即根因类 bug 的来源。

## 目标

1. 新增 `src/flow-map/projection/`（模块前缀 **`proj`**）
2. **Gate 策略表**：`projReadGatePolicy` — confirmRoute 才 prune；validate/save 只投影
3. **ProjSpec Registry**：镜像 `electron/transitions/registry.ts`
4. **timelineReadRunParent**：startRun 与 runTimeline scope 一致

## Gate 策略

| Gate | pruneWorkerSubtree | projectWorkerMode |
|------|---------------------|-------------------|
| confirmRoute | true | routesOnly |
| validate | false | draftFlags |
| save | false | numberedPersist |

## ProjSpec

| key | anchor | pruneKinds | resetDefaultNews |
|-----|--------|------------|------------------|
| 0-1 | source chain | — | — |
| 1-2 | news | subAgent, claim, opinion | news:default |
| 2-3 | claim | subAgent, opinion | — |

## 非目标

- 不合并 LangGraph split/verify state
- 不改 IPC channel
