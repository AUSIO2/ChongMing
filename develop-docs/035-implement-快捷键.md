# 035-implement — 快捷键

## 步骤

- [x] `flow-map/layout-nav.ts` + 单测
- [x] `src/shortcuts/`：`read-ignore`、`format-hint`、`use-app-shortcuts`
- [x] store：`selectLayoutNeighbor`、`runOrContinuePrimary`
- [x] `HomeView` 挂载快捷键；`FlowMapTopology` 视口跟随
- [x] `FlowMapTimelinePlayer` 主按钮 title hint
- [x] counter=35

## 验证

```bash
npm run test:map
```
